[pytest]
pythonpath = .
testpaths = tests

# Integration: LLM call
markers =
    integration: gọi service thật (Ollama, PostgreSQL...) — không chạy mặc định, cần flag -m integration
addopts = -m "not integration"